<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:EcommerceMaui.ViewModels;assembly=EcommerceMaui.Core"
             xmlns:m="clr-namespace:EcommerceMaui.Models;assembly=EcommerceMaui.Core"
             x:Class="EcommerceMaui.Views.SearchPage"
             x:DataType="vm:SearchViewModel"
             Title="{Binding Title}">

    <Grid RowDefinitions="Auto,Auto,Auto,Auto,*" Padding="24" RowSpacing="12">

        <Label Grid.Row="0"
               Text="Search results"
               FontSize="28"
               FontAttributes="Bold" />

        <Label Grid.Row="1"
               Text="{Binding Query, StringFormat='for &quot;{0}&quot;'}"
               FontSize="16"
               TextColor="{AppThemeBinding Light=#555555, Dark=#BBBBBB}" />

        <Label Grid.Row="2"
               Text="{Binding ResultsMessage}"
               IsVisible="{Binding ResultsMessage, Converter={StaticResource NotNullConverter}}"
               TextColor="{AppThemeBinding Light=#777777, Dark=#999999}" />

        <ActivityIndicator Grid.Row="3"
                           IsRunning="{Binding IsBusy}"
                           IsVisible="{Binding IsBusy}" />

        <CollectionView Grid.Row="4"
                        ItemsSource="{Binding Results}"
                        SelectionMode="None">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="m:Product">
                    <Border Stroke="{AppThemeBinding Light=#DDDDDD, Dark=#444444}"
                            StrokeThickness="1"
                            Padding="16"
                            Margin="0,0,0,12">
                        <VerticalStackLayout Spacing="6">
                            <Label Text="{Binding Name}"
                                   FontAttributes="Bold"
                                   FontSize="18" />
                            <Label Text="{Binding Description}"
                                   FontSize="14" />
                            <Label Text="{Binding Price, StringFormat='{0:C}'}"
                                   FontSize="16"
                                   FontAttributes="Bold" />
                            <Button Text="View Details"
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type vm:SearchViewModel}}, Path=ViewDetailsCommand}"
                                    CommandParameter="{Binding .}" />
                        </VerticalStackLayout>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>
