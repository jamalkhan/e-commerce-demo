<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:EcommerceMaui.ViewModels;assembly=EcommerceMaui.Core"
             x:Class="EcommerceMaui.Views.LoginPage"
             x:DataType="vm:LoginViewModel"
             Title="{Binding Title}">

    <ScrollView>
        <VerticalStackLayout Padding="24" Spacing="16">

            <ActivityIndicator IsRunning="{Binding IsBusy}"
                               IsVisible="{Binding IsBusy}" />

            <Label Text="{Binding ErrorMessage}"
                   TextColor="Red"
                   IsVisible="{Binding HasError}" />

            <VerticalStackLayout Spacing="8"
                                 IsVisible="{Binding WelcomeMessage, Converter={StaticResource NotNullConverter}}">
                <Label Text="Welcome"
                       FontSize="28"
                       FontAttributes="Bold" />
                <Label Text="{Binding UserName}"
                       FontSize="20" />
                <Label Text="{Binding WelcomeMessage}"
                       FontSize="14"
                       TextColor="{AppThemeBinding Light=#555555, Dark=#BBBBBB}" />
            </VerticalStackLayout>

            <Button Text="Back to Home" Command="{Binding BackCommand}" />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
